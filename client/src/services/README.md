# Services

Use this folder for client-side service orchestration that is not a direct HTTP API wrapper.

Examples:

- realtime stream clients
- AI provider adapters
- chart data normalization
- portfolio calculation services

Direct REST wrappers currently live in `src/api`.
