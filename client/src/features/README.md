# Features

Feature-level modules should live here as the app is split out of legacy page/component files.

Recommended slices:

- `charting`
- `portfolio`
- `orders`
- `watchlist`
- `ai`
- `settings`

Keep route pages thin and place feature-specific state, hooks, and UI composition in these folders.
